@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    <ul class="nav navbar-nav navbar-right">
        @*@if (User.IsInRole("Administrator"))
        {
            <li>@Html.Bootstrap().ActionLink("Panel administratora", MVC.Account.Users.Index())</li>
        }*@
        <li>@Html.ActionLink(JMap.Identity.Manage.Index())</li>
        <li>@Html.ActionLink(JMap.Identity.Account.LogOff())</li>
    </ul>
}
else
{
    <ul class="nav navbar-nav navbar-right">
        <li>@Html.ActionLink(JMap.Identity.Account.Register())</li>
        <li>@Html.ActionLink(JMap.Identity.Account.Login())</li>
    </ul>
}